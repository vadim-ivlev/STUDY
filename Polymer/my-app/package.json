{
  "name": "my-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "doc"
  },

  "scripts": {
    "push": "git add -A . && git commit -am 'v' && git push",
    "stylus": "stylus css/index.styl",
    "stylusw": "stylus -w css/index.styl",
    "jade": "jade jade -P -o .",
    "jadew": "jade jade -P -w -o .",
    "coffee": "browserify -d -t coffeeify coffee/main.coffee -o static/bundle.js",
    "coffeew": "watchify  -d -t coffeeify coffee/main.coffee -o static/bundle.js",
    "watch": "npm run jadew & npm run coffeew & npm run stylusw &",
    "live-reload": "live-reload  .  --port=9090 --delay=500",
    "dev0": "npm run live-reload & npm run start & npm run watch & echo 'Launch browser and start editing files' ",
    "dev": "npm run live-reload & npm run start & echo 'Launch browser and start editing files' ",
    "start": "http-server --cors -s -o",
    "serve": "node server.js",
		"stop": "killall node"
  },


  "author": "",
  "license": "ISC",
  "dependencies": {
    "live-reload": "^1.1.0"
  }
}
